{
  "schemaVersion": 1,
  "id": "ianswope.apple-music",
  "name": "Apple Music",
  "version": "0.1.0",
  "author": "Ian Swope",
  "license": "MIT",
  "description": "Apple Music as a standalone player on Omarchy: its own window, its own MPRIS player, now playing and transport controls in the bar.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Apple Music",
    "description": "Now playing and transport for Apple Music, scoped to its own browser instance so the rest of your tabs never appear here.",
    "category": "Media",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "showTrackInBar": true,
      "maxBarChars": 28,
      "showArtwork": true,
      "refreshIntervalSec": 10
    },
    "schema": [
      {
        "key": "showTrackInBar",
        "type": "boolean",
        "label": "Show the track beside the bar icon",
        "defaultValue": true
      },
      {
        "key": "maxBarChars",
        "type": "integer",
        "label": "Characters of track text in the bar",
        "min": 8,
        "max": 80,
        "step": 2,
        "defaultValue": 28
      },
      {
        "key": "showArtwork",
        "type": "boolean",
        "label": "Show album artwork in the panel",
        "defaultValue": true
      },
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "How often to check for the player (seconds)",
        "min": 3,
        "max": 120,
        "step": 1,
        "defaultValue": 10
      }
    ]
  }
}
